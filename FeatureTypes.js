/**
 * @fileoverview Defines the data for all feature types in the game.
 * Features are geographical elements that can appear on top of a biome.
 */

export const FeatureTypes = {
  FOREST: {
    id: 'forest',
    name: 'Forest',
    // Example properties that could be added:
    // movementCostModifier: 1,
    // provides: { resource: 'wood', amount: 10 },
    // blocksPlacement: ['Residence']
  },
  // Other features like 'Hills', 'Jungle', 'Oasis' could be added here.
};